on: 
  issues:
    types: [opened]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Add Sponsor Labels
    steps:
    - name: Add Sponsor Labels
      id: sponsors-labels
      uses: brianlagunas/sponsor-action@v0.30
      with:
        maintainers: 'brianlagunas'
        github_token: ${{ secrets.GITHUB_TOKEN }}